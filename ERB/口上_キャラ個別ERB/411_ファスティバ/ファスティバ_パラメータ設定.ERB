;****************************************
;キャラの初期パラメータを口上側で制御するためのファイル
;****************************************
;・使い方
;「411」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;初期パラメータ変更
;--------------------------------------------------
@口上側初期パラメータ変更_411
;NEWGAME時に呼ばれる関数、ロード時には呼ばれないことに注意
;TALENTをONOFFしたり、EXPの取得や初期ABLの変更などをここに記述すること
; LOCAL = GETCHARA(411)

; ;キャラクターが自分を指す際の呼び方
; SIF CSTR:LOCAL:一人称 == ""
; 	CSTR:LOCAL:一人称 = 

; ;キャラクターがあなたを指す際の呼び方
; SIF CSTR:LOCAL:二人称 == ""
; 	CSTR:LOCAL:二人称 = 

; ;キャラクターが固有に持つ独自の好感度要素の名前
; ;通常上昇しないので、設定した場合はキャラ固有処理_411.ERBなどで上昇処理も作ること
; CSTR:LOCAL:キャラ固有好感度要素 = 

; ;プレゼントの好み
; ;基本的にはCSVで設定すべきだが、口上特有の設定をする場合はここに記述
; ;CSTR:LOCAL:プレゼント好み = 

; ;身長やバストの数値を指定する・変更する場合
; ; BASE:LOCAL:身長 = 
; ; BASE:LOCAL:バスト = 

;性別の表記変更
;「男の娘」や「漢女」などを表記するための変数
CSTR:LOCAL:性別表記 = 漢女

;--------------------------------------------------
;ロード時パラメータ変更
;--------------------------------------------------
@口上側LOADENDパラメータ変更_411
;ロード時に呼ばれる変数。NEWGAME時に呼ばれないことに注意
;初期パラメータ関数だけだと後から追加した時にバグるため追加
LOCAL = GETCHARA(411)

; ;キャラクターが自分を指す際の呼び方
; IF CSTR:LOCAL:一人称 == ""
; 	CSTR:LOCAL:一人称 = %CSVCSTR(NO:LOCAL, GETNUM(CSTR, "一人称"))%
; ENDIF
; ;キャラクターがあなたを指す際の呼び方
; IF CSTR:LOCAL:二人称 == ""
; 	CSTR:LOCAL:二人称 = %CSVCSTR(NO:LOCAL, GETNUM(CSTR, "二人称"))%
; ENDIF

; ;キャラクターが固有に持つ独自の好感度要素の名前
; ;通常上昇しないので、設定した場合はキャラ固有処理_411.ERBなどで上昇処理も作ること
; IF CSTR:LOCAL:キャラ固有好感度要素 == ""
; 	CSTR:LOCAL:キャラ固有好感度要素 = 
; ENDIF

;性別の表記変更
;「男の娘」や「漢女」などを表記するための変数
CSTR:LOCAL:性別表記 = 漢女
